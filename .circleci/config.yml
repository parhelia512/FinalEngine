version: 2
jobs:
  build:
    docker:
      - image: microsoft/dotnet:3.0-sdk
    steps:
      - checkout
      - run:
          name: Restore
          command: dotnet restore
      - run:
          name: Build
          command: dotnet build --no-restore